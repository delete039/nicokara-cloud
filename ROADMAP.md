# ニコカラ自动生成器 Cloud 路线图

> 当前基线：v0.2.0
> 文档状态：持续更新
> 更新日期：2026-08-05

## 产品边界

Cloud 面向没有字幕制作和视频剪辑经验的用户，以浏览器内的简单操作、稳定排队和较高吞吐为优先目标。

- 默认流程必须保持少参数和自动化。
- 新功能不得长期占用上传槽位或处理 worker。
- 交互式检查期间应释放 worker，保存中间结果后等待用户继续。
- 高计算量功能必须可选、可限额、可超时并具有降级路径。
- 复杂时间轴编辑、任意字幕特效和批量制作保留给 Studio。

## 版本规划

### v0.2.0 - 已完成（P0 后台监控与队列稳定性）

目标：管理员能够立即回答“谁在等待、谁在上传、谁在处理、任务卡在哪里”。

- 增加需要管理员认证的监控页面，并提供带审计记录的取消和重新入队操作。
- 分别展示 `WAITING`、`READY`、`UPLOADING`、`UPLOADED` 和 `PROCESSING` 数量。
- 展示上传槽位、处理 worker、任务阶段、阶段持续时间及最近错误。
- 增加 worker 心跳和队列健康检查；现有基础 `/health` 只作为 API 存活检查。
- 自动识别并释放失去心跳的 `READY`、`UPLOADING` 上传票据。
- 提供管理员取消、重新入队和查看日志关联 ID 的操作。
- 增加结构化日志，统一记录 `ticket_id`、`job_id`、阶段、耗时和错误码。
- 展示 CPU、内存、磁盘空间及共享存储占用，不公开用户素材内容。

验收标准：

- 无需查询 SQLite 即可看到上传等待队列和处理队列。
- 异常关闭浏览器不会长期占用唯一上传槽位。
- worker 停止消费队列时，监控页能够明确显示异常。
- 管理员操作均记录审计信息，普通用户无法访问监控接口。

### v0.3.0 - P0 上传可靠性与异常恢复

目标：大文件上传能够恢复，失败原因能够定位，并避免耗尽磁盘。

- 完善分片上传断点续传，刷新页面后恢复上传票据和已完成分片。
- 服务端校验分片范围、总大小、文件 SHA-256 和最终合并结果。
- 上传前使用 FFprobe 检查时长、编码、分辨率和音视频轨道。
- 在接收文件前检查可用磁盘空间并预留安全余量。
- 区分网络中断、票据过期、代理超时、空间不足和格式错误。
- 缩短异常上传槽位的回收周期，同时保留活跃上传心跳。
- 增加上传恢复、重复提交和服务重启的集成测试。

验收标准：

- 网络中断后可以继续上传，不必重新传输已完成分片。
- 所有失败均有面向用户的解决方法和面向管理员的诊断信息。
- 上传失败不会残留永久占位或无法清理的临时文件。

### v0.4.0 - P1 LRC 快速通道与整体偏移

目标：已有时间轴的用户能够更快生成视频。

- 支持普通 LRC、增强型逐字 LRC及现有 TXT 歌词。
- 时间统一保存为整数毫秒，避免多次换算产生浮点误差。
- 增加“字幕整体提前/延后”设置，并提供正负方向示例。
- 普通 LRC 的行时间作为对齐边界，行内继续进行 Mora 分配或必要的局部对齐。
- 增强型 LRC 在校验通过时尽量复用逐字时间，跳过不必要的计算步骤。
- 生成前显示轻量时间轴摘要，不增加完整编辑器。
- 支持导出处理后的 LRC、ASS 和版本化项目 JSON。

验收标准：

- LRC 快速通道的平均计算量不高于当前 TXT 自动对齐流程。
- 时间偏移不会产生负时间、逆序时间或超出视频时长的字幕。
- 不支持或损坏的 LRC 能明确提示并安全退回 TXT 流程。

### v0.5.0 - P1 字幕预设与 Kirakara 兼容

目标：在不显著增加渲染成本的前提下提供有限的视觉选择。

- 提供 3 至 5 个经过性能验证的字幕预设。
- 允许选择主色、走字后颜色和经过白名单验证的服务器字体。
- 限制高成本阴影、模糊、复杂动画及任意字体上传。
- 定义版本化 `nicokara-project` JSON 交换格式。
- 输出可被 Kirakara-Player 使用的 LRC、Ruby 和媒体关联信息。
- 服务端 FFmpeg 继续作为 Cloud 最终成片的权威渲染器。

验收标准：

- 所有预设在目标分辨率下通过可读性、安全区域和渲染耗时测试。
- 样式选择不会改变时间轴数据。
- 导出文件可以在 Kirakara-Player 工作流中打开或经过明确的一步转换后打开。

### v0.6.0 - P2 轻量质量检查

目标：允许用户处理特殊读音和明显错轴，同时保持 Cloud 的简单定位。

- 标记低置信度注音、训读、熟字训、古日语和专有名词候选。
- 默认继续自动生成；用户可选择进入检查流程。
- 检查状态使用 `AWAITING_REVIEW`，不得占用处理 worker。
- 仅提供注音修改、单行时间修改和整体偏移，不提供完整波形编辑器。
- 用户确认后创建独立渲染任务，支持超时清理未完成草稿。

验收标准：

- 等待用户检查时不降低其他用户的生成吞吐。
- 修正结果可以导出，并能由 Studio 继续编辑。
- 未选择检查的用户仍可使用原有一键生成流程。

### v0.7.0 - P2 可选高精度对齐

目标：在证据充分时引入 FA-Kara 或其他强制对齐模型。

- 建立可插拔对齐引擎接口，保留当前引擎作为默认和降级路径。
- 使用固定日语歌曲测试集比较 Mora 偏差、漏字率、失败率、耗时和峰值内存。
- 复用已有的人声分离产物，避免同一任务重复分离音频。
- FA-Kara 仅作为可选“高精度模式”，设置独立超时和资源配额。
- 模型失败时回退当前引擎，并在结果中记录实际使用的引擎版本。
- 引入前审查 FA-Kara、MMS_FA、模型权重、字典和所有间接依赖的许可证。

验收标准：

- 只有在质量提升达到预设门槛且资源成本可接受时才向公开用户开放。
- 高精度任务不会阻塞普通快速任务队列。
- 管理员可以独立关闭实验引擎而不影响默认流程。

### v1.0.0 - 稳定公开版本

- 上传队列、处理队列、恢复、取消和清理机制经过长期运行验证。
- LRC、TXT、ASS 和项目 JSON 的格式契约稳定并有迁移策略。
- 核心指标、告警、备份、恢复和部署回滚文档齐全。
- 默认流程仍保持少步骤，不依赖人工编辑即可完成生成。
- 所有公开功能具有资源上限、错误反馈和自动化测试。

## Cloud 明确不纳入默认流程的功能

- 完整音频波形和 Mora 级拖拽编辑器。
- 无限制字体、滤镜、阴影和动画编辑。
- 默认启用高计算量对齐模型。
- 依赖浏览器编码能力作为唯一成片方式。
- 在用户编辑或检查期间持续占用后台 worker。

## 跨版本公共契约

Cloud 与 Studio 应共同维护版本化项目格式，至少包含：

- 素材元数据和校验值。
- 行、词和 Mora 级时间信息。
- 原文、注音、用户修正及置信度。
- 全局和局部时间偏移。
- 字幕样式引用及字体回退信息。
- 对齐引擎、模型、参数和生成版本。
- LRC、ASS、Kirakara 兼容数据及最终视频产物索引。

## 参考项目

- [FA-Kara](https://github.com/moriwx/FA-Kara)
- [Kirakara-Player](https://github.com/FMPeach/Kirakara-Player)
- [Kirakara-Player 在线版本](https://rl.fmpeach.top/)
